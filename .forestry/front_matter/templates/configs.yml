---
label: Configs
hide_body: true
fields:
- type: field_group
  name: home
  label: Home
  fields:
  - type: list
    name: tabs
    label: Tabs
    default: []
    config:
      use_select: false
- type: field_group
  name: files
  label: Files
  fields:
  - type: select
    name: display
    label: Default Display
    default: []
    config:
      required: true
      options:
      - list
      - grid
      source:
        type: simple
        section:
        file:
        path:
pages:
- _data/configs.yml
