// Import Plugins
import $ from 'jquery'
import boostrap from 'bootstrap'
import '@fortawesome/fontawesome-free/js/all'
import Popper from 'popper.js';
window.Popper = Popper;

// Import Components
import Navbar from "./components/navbar.js";

// window.App = {}
// $.widget.bridge('appNavbar', Navbar);

// const onDOMReady = () => $('#header').appNavbar();

// $(document).
//   ready(onDOMReady).
//   on('page:load', onDOMReady);